# Instructions for Claude

- Never add Claude attribution to commits, pull requests or GitHub comments:
  no `Co-Authored-By: Claude` or `Claude-Session:` trailers, no
  "Generated with Claude Code" line and no link to the Claude conversation.
- Run `npm test` before pushing.
